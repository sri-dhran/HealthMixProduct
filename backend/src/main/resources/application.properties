server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/healthmixdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=sridharank
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# ============================================
# Gmail SMTP Configuration for OTP Emails
# IMPORTANT: Use a Gmail App Password, NOT your regular Gmail password.
# Steps to generate an App Password:
#   1. Go to myaccount.google.com
#   2. Enable 2-Step Verification (if not already)
#   3. Go to Security > App passwords
#   4. Create an app password for "Mail" on "Windows Computer"
#   5. Paste the 16-character code below (remove spaces)
# ============================================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sri741815@gmail.com
spring.mail.password=zvqovcdxgtvggkwq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=3000
spring.mail.properties.mail.smtp.writetimeout=5000
